{{ attach_library('theme_name/sample-list') }}
<section class="sample-list">
  <h2 class="sample-list__heading">{{ heading }}</h2>
  {% for item in items %}
  <article class="sample-list__item">
    <h3>{{ item.title }}</h3>
    <p>{{ item.desc }}</p>
  </article>
  {% endfor %}
</section>
